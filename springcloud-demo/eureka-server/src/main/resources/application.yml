server:
  port: 10086
spring:
  application:
    name: eureka-server # 应用名称会在eureka中显示
eureka:
  client:
    service-url: # EurekaServer的地址
      defaultZone: http://127.0.0.1:10086/eureka
    register-with-eureka: false # 不注册自己
    fetch-registry: false # 不下拉自己
